# Feature Modules

This folder is the entry point for feature-oriented frontend modules.

Suggested structure per feature:

- `api/` for service calls
- `components/` for feature-local UI
- `hooks/` for reusable feature behavior
- `types/` for feature-specific TypeScript types
