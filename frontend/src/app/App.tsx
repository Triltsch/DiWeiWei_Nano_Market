import { AppShell } from "../shared/ui/AppShell";

export function App(): JSX.Element {
  return (
    <AppShell>
      <h1>DiWeiWei Nano Market</h1>
      <p>Frontend baseline is ready for Sprint 2 feature modules.</p>
    </AppShell>
  );
}
